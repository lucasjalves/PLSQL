SET SERVEROUTPUT ON;
DECLARE
	x		INTEGER := 3100;
	result 	LOCATIONS%ROWTYPE;
BEGIN
	EXECUTE IMMEDIATE 'SELECT * FROM LOCATIONS WHERE LOCATION_ID = ' || x
	INTO result;
	dbms_output.put_line(result.city);
END;
/